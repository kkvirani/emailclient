import { LayoutDashboard } from "lucide-react";
import { ComingSoon } from "@/components/common/coming-soon";

export const metadata = { title: "Dashboard — Postal" };

export default function Page() {
  return <ComingSoon icon={LayoutDashboard} title="Analytics Dashboard" phase="Phase 8" />;
}
