import { MailX } from "lucide-react";
import { ComingSoon } from "@/components/common/coming-soon";

export const metadata = { title: "Unsubscribe — Postal" };

export default function Page() {
  return <ComingSoon icon={MailX} title="Unsubscribe Center" phase="Phase 6" />;
}
